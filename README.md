# Jain Kalyanam — Profile Search & Registration

A static HTML/CSS/JS mockup of a matchmaking profile registry: filterable search results, a biodata detail view, a create/edit profile wizard, and an admin notes & preferences panel.

Open `index.html` directly, or view it live via GitHub Pages once enabled.
